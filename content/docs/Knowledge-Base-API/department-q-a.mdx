---
title: 部門問答 API
description: 根據部門資料庫進行問答
full: true
method: POST
route: /dept_QA
---

<APIPage operations={[{"path":"/dept_QA","method":"post"}]} hasHead={false} />

export function startup() {
    if (toc) {
        // toc might be immutable
        while (toc.length > 0) toc.pop()
        toc.push(...[])
    }
    
    if (structuredData) {
        structuredData.headings = []
        structuredData.contents = [{"content":"根據部門資料庫進行問答"}]
    }
}

{startup()}