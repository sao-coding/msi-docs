---
title: 搜尋 API
description: 搜尋用戶查詢的相關信息
full: true
method: POST
route: /search
---

<APIPage operations={[{"path":"/search","method":"post"}]} hasHead={false} />

export function startup() {
    if (toc) {
        // toc might be immutable
        while (toc.length > 0) toc.pop()
        toc.push(...[])
    }
    
    if (structuredData) {
        structuredData.headings = []
        structuredData.contents = [{"content":"搜尋用戶查詢的相關信息"}]
    }
}

{startup()}